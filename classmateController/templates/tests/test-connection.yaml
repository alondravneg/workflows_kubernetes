apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "classmateController.fullname" . }}-test-connection"
  labels:
    {{- include "classmateController.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "classmateController.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
